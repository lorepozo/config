[user]
    name = Lucas Morales
    email = lucas@lucasem.com
    signingkey = AB31E959F7559E19
[commit]
    gpgsign = true
[alias]
    lol = log --all --graph --date-order --pretty=format:'%C(yellow)%h%C(bold red)%d%Creset %s %Cgreen{%an} %Cblue%cr%Creset' --abbrev-commit
    lob = log               --date-order --pretty=format:'%C(yellow)%h%C(bold red)%d%Creset %s %Cgreen{%an} %Cblue%cr%Creset' --abbrev-commit
    l = lol
    lat = !git fetch -a && git checkout origin/master
    a = add
    amend = !git commit --amend --no-edit --date=\"$(date -R)\"
    b = branch -vv
    bmv = !git-bmv
    c = commit
    co = checkout
    cp = cherry-pick
    cpr = !git-cpr
    d = diff
    da = diff -U99999
    del = !git-del
    ds = diff --staged
    f = fetch
    nb = !git-nb
    o = checkout
    r = rebase
    s = status
[core]
    pager = diff-highlight | less --tabs=4 -RFX
    excludesfile = ~/.gitignore
[color]
    ui = true
[diff]
    compactionHeuristic = true
[pull]
    rebase = true
