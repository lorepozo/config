# ~/.xprofile
# used in manjaro (with xfce) instead of xinit

setxkbmap -option ctrl:nocaps
xinput set-prop bcm5974 "libinput Natural Scrolling Enabled" 1
xinput set-prop bcm5974 "libinput Tapping Drag Enabled" 0
eval $(ssh-agent -s)
